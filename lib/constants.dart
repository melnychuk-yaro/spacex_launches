import 'package:flutter/material.dart';

const kBorderRadius = 16.0;
const kShadow = BoxShadow(
  color: Color.fromARGB(64, 00, 00, 16),
  spreadRadius: 0,
  blurRadius: 30,
  offset: Offset(0, 8), // changes position of shadow
);
